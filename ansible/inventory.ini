[raisetech_host]
raisetech13 ansible_host={{ansible_server_host}}

[raisetech_host:vars]
# 変数（下記変更で他のファイルにも変更がかかる）
ansible_user=ec2-user
ansible_ssh_private_key_file={{ key_name }}

# 開通コマンド例 ansible raisetech_host -m ping -i inventory.ini -e "ansible_server_host=10.0.0.1" -e "~/.ssh/ec2no.pem"
